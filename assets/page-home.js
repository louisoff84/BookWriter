(async()=>{const {api,$,bookCard}=BW;const grid=$('#featured-books');if(!grid)return;try{const d=await api('public/books?limit=4');grid.textContent='';if(!d.books.length){grid.innerHTML='<div class="empty-inline">Les premiers livres publiés apparaîtront ici.</div>';return}d.books.forEach(b=>grid.append(bookCard(b)))}catch{grid.innerHTML='<div class="empty-inline">La bibliothèque sera disponible dès que l’API sera en ligne.</div>'}})();
